


const defaultReducer = (state = [], action) => {
    const { type, payload} = action
    switch (type) {
        case value:
         
    
        default:
            return state
    }
}

export default defaultReducer